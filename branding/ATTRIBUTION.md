# Branding attribution

The B.S. Awareness brand mark and BSA-specific marker assets in this folder are project artwork.

Application interface icons should use the project's Lucide dependency where practical rather than copying Lucide SVGs into this branding package.

The selected visual reference used to derive this package is preserved at `reference/branding-reference.png`.
